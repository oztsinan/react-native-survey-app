import { ScrollView, Text, View } from "react-native";

export const BottomTabProfileScreen = () => {
  return (
    <ScrollView contentInsetAdjustmentBehavior="automatic">
      <Text>BottomTabProfileScreen</Text>
    </ScrollView>
  );
};
